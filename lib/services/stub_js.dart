// 모바일 환경을 위한 더미 구현
class JsObject {
  void callMethod(String method, List<dynamic> args) {
    // 모바일에서는 아무 동작도 하지 않음
  }
}

class Context {
  void callMethod(String method, List<dynamic> args) {
    // 모바일에서는 아무 동작도 하지 않음
  }
}

final context = Context();